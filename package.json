{
  "name": "lord-sorgu",                  // Projenin adı
  "version": "1.0.0",                     // Projenin versiyonu
  "description": "Sorgu API Proxy",       // Proje açıklaması
  "main": "server.js",                    // Uygulama başlatma dosyası
  "scripts": {
    "start": "node server.js"             // Projeyi çalıştırmak için komut
  },
  "dependencies": {
    "express": "^4.17.1",                 // Express.js bağımlılığı
    "node-fetch": "^2.6.1"                // HTTP isteği yapmak için fetch kullanımı
  }
}
